BRANCH="$(git rev-parse --abbrev-ref HEAD)"

if [ "$BRANCH" == "master" ] || [ "$BRANCH" == "develop" ]; then
  echo "Do not commit directly to master or develop branches"
  exit 1
fi

npm run format && npm run lint:fix && npm test
